# wiki — 프로젝트 작업 지침

<!-- WORKSPACE-COMMON:BEGIN v2 -->

이 파일은 멀티 저장소 워크스페이스에서 Codex와 Claude Code가 각각 선택적으로 사용하는 공통 프로젝트 계약이다.

## 우선순위와 범위

- 이 저장소의 기존 `AGENTS.md`, `CLAUDE.md`, `README` 및 공식 문서를 먼저 확인한다.
- 이 템플릿은 워크스페이스 기본값이다. 저장소에 더 구체적인 규칙이 있으면 그 규칙과 사용자의 명시적 지시를 우선한다.
- 현재 저장소 루트는 이 파일이 있는 디렉터리다. 다른 저장소는 독립된 작업 공간으로 취급한다.
- 현재 저장소 밖의 파일을 변경하지 않는다. 여러 저장소 변경이 필요하면 대상과 이유를 먼저 보고한다.
- 중앙 워크스페이스는 `src`, `release`, Deploy Agent 또는 자동 배포 경로를 제공하지 않는다. 저장소 자체의 배포 규칙은 사용자가 명시적으로 요청한 경우에만 따른다.

## 작업 시작

1. 요청과 관련된 저장소 규칙·README·설계 문서를 확인한다.
2. `git status --short`, 현재 브랜치, 최근 커밋을 확인해 기존 변경을 보존한다.
3. 이번 작업의 `orchestrator`를 `Claude` 또는 `Codex` 중 하나로 선언하고, 프로젝트 세션·하위 에이전트·리뷰 세션을 같은 도구로 배정한다.
4. 변경 범위와 검증 방법을 짧게 정리한 뒤 작업한다.
5. 모호하거나 파괴적인 작업은 추측하지 말고 사용자에게 확인한다.

## 프로젝트 문서화 및 요구사항 확인

- 프로젝트 관련 문서는 항상 현재 저장소의 `docs\`에 둔다.
- 코드·설정·동작을 변경하는 작업은 다음 세 문서를 같은 `<slug>`로 만든다. 단순 질의나 읽기 전용 답변은 예외다.
  - `*-analysis.md` (예: `<slug>-analysis.md`): 요청, 현재 상태와 근거, 부족한 정보와 질문/답변, 가정, 범위·비범위, 완료 기준
  - `*-design.md` (예: `<slug>-design.md`): 확인된 요구사항, 구조·데이터·제어 흐름, 인터페이스, 대안과 트레이드오프, 보안·호환성, 검증 전략
  - `*-tasks.md` (예: `<slug>-tasks.md`): 실행 작업 목록, 의존성, 담당 도구, model, effort, 병렬 그룹, 변경 파일, 검증 방법, 상태
- `<slug>`은 저장소 안에서 유일한 소문자 kebab-case 영문 식별자로 정한다. 기존 `docs\README.md` 또는 문서 인덱스가 있으면 새 문서를 연결한다.
- 모르는 내용이나 부족한 요구사항은 구현 전에 한 번에 모아 사용자에게 질문한다. 답변과 결정 근거를 `*-analysis.md`에 기록하며, 핵심 답변이 없으면 `blocked` 상태로 두고 구현하지 않는다.
- 분석·설계 문서는 추측으로 확정하지 않는다. 중요한 변경은 두 문서 작성 후 사용자 확인을 받은 다음 작업계획과 구현으로 진행한다.
- 작업 중 결정·범위·검증 결과가 바뀌면 세 문서를 함께 갱신한다. 토큰·키·비밀번호 등 비밀정보는 문서에 기록하지 않는다.

## 웹 페이지 및 UX/UI 작업 게이트

- 웹 페이지의 기본 호스팅·프리뷰·배포 대상은 Vercel이다. 배포 실행은 사용자가 명시적으로 요청한 경우에만 하며, release 경로나 Deploy Agent를 만들지 않는다.
- 동적 데이터·인증·스토리지·실시간 기능은 Supabase를 기본 백엔드로 사용한다. 완전한 정적 페이지라 Supabase가 필요하지 않으면 analysis 문서에 제외 근거를 기록한다.
- Supabase 연동은 공개 클라이언트에 publishable/anon 범위의 키만 사용하고, service_role·secret 키를 노출하지 않는다. 노출 스키마 테이블에는 RLS와 실제 접근 정책을 적용하고 패키지 버전과 lockfile을 고정한다.
- 웹 페이지 신규 제작 또는 주요 UX/UI 변경은 운영 구현 전에 서로 다른 콘셉트의 샘플 페이지 3종을 만든다. 콘셉트는 테마·레이아웃·타이포그래피·모션 방향이 실질적으로 달라야 하며, 색상만 바꾼 변형은 허용하지 않는다.
- 샘플은 docs/ux-concepts/<slug>/concept-01, concept-02, concept-03에 실행 가능한 프로토타입 또는 확인 가능한 프리뷰로 남긴다. 동일한 요구사항과 콘텐츠를 사용하고 데스크톱·모바일·기본 접근성을 확인한다.
- 사용자에게 3종을 제시해 명시적인 선택·피드백을 받기 전에는 운영 코드, 실제 데이터 연동, 주요 구현을 시작하지 않는다. 선택 결과와 피드백을 analysis/design/tasks 문서에 기록하고, 미확인 상태는 blocked로 유지한다. 면제는 사용자가 명시한 경우만 허용한다.
- UX/UI 작업에는 impeccable과 현재 기본 taste 스킬인 design-taste-frontend를 필수로 사용한다. 각 SKILL.md의 setup·설계·pre-flight 지침을 먼저 읽고 적용하며, 다른 taste 변형을 사용할 때는 근거를 기록하고 한 작업에서 여러 변형을 섞지 않는다.
- Vercel 또는 Supabase의 설정·CLI·배포·스키마 작업은 해당 플랫폼의 현재 공식 문서와 설치된 관련 스킬을 확인한 뒤 수행한다.

## 작업계획, model/effort 및 병렬 실행

- 구현 전에 반드시 `*-tasks.md`를 만들고 모든 작업에 아래 필드를 채운다.
  - `orchestrator`: 이 작업 트리를 조정하는 도구 (`Claude` 또는 `Codex`)
  - `owner`: `Claude` 또는 `Codex`
  - `model`: 선택한 오케스트레이터가 지원하는 모델
  - `effort`: `low`, `medium`, `high`, `xhigh`, `max` 중 하나
  - `depends_on`, `parallel_group`, `files`, `verification`, `status`
- 기본 배정 기준은 다음과 같다. 작업 성격에 따라 조정하되 tasks 문서에 선택 이유를 남긴다.
  - `orchestrator=Claude`: 설계·복잡한 디버깅·리뷰는 `opus`/`sonnet`, 구현·테스트·문서 작업은 `sonnet`, 단순 탐색은 `haiku`를 사용한다.
  - `orchestrator=Codex`: 설계·복잡한 디버깅·리뷰는 선택한 고성능 Codex 모델과 높은 effort, 구현·테스트는 중간 effort, 단순 탐색은 낮은 effort를 사용한다.
  - 한 작업계획 안에서는 위 두 프로필을 섞지 않는다. 다른 도구의 교차 리뷰가 필요하면 별도 작업계획과 명시적 핸드오프로 분리한다.
- 기본 실행 방식은 독립 작업을 서브에이전트로 위임하고 `parallel_group` 단위로 병렬 진행하는 것이다.
- `depends_on`이 해결되지 않았거나 같은 파일·스키마·마이그레이션·공유 리소스를 쓰는 작업은 병렬 실행하지 않는다. 이런 경우 순차 실행 이유를 tasks 문서에 적는다.
- 부모 세션은 작업 분해·상태 수집·충돌 조정·최종 취합을 담당한다. 서브에이전트 결과를 그대로 신뢰하지 말고 핵심 변경과 검증을 직접 확인한다.
- 작업 시작·완료·차단·재시도 상태를 `*-tasks.md`에 갱신한다. 병렬 작업이 모두 끝나기 전에는 통합 작업을 시작하지 않는다.

## 오케스트레이터와 세션 도구 일관성

- 하나의 작업 트리에서는 루트 오케스트레이터와 프로젝트별 세션·하위 에이전트·리뷰 세션을 같은 도구로 유지한다.
- `orchestrator=Claude`이면 모든 작업의 `owner`를 Claude로, `orchestrator=Codex`이면 모든 작업의 `owner`를 Codex로 지정한다.
- Claude와 Codex 사이의 자동 양방향 위임이나 동시 실행을 전제로 하지 않는다. 도구를 바꿔야 하면 현재 작업을 정리하고 핸드오프 문서와 새 오케스트레이터를 명시한 별도 작업으로 시작한다.
- 같은 파일을 여러 세션이 동시에 수정하지 않는다. 같은 도구를 사용하더라도 작업 경계와 파일 소유권을 분명히 한다.

## 편집 및 Git 안전

- 사용자의 기존 변경을 덮어쓰지 않는다. `git reset --hard`, 무단 checkout, 광범위한 삭제를 사용하지 않는다.
- `main`/`develop`에 직접 커밋하지 않는다. 작업 브랜치와 Conventional Commits를 사용한다.
- 사용자가 명시적으로 요청하기 전에는 commit, push, PR, merge를 수행하지 않는다.
- 요청 범위를 벗어난 리팩터링·의존성 업그레이드·포맷 일괄 변경을 하지 않는다.
- 코드 변경 후 저장소에 맞는 최소 검증을 실행하고 실패 원인을 숨기지 않는다.

## 보안 및 비밀정보

- 토큰·API 키·비밀번호·개인 키·`.env` 값은 출력·커밋·외부 전송하지 않는다.
- 로그·스크린샷·테스트 산출물에 비밀정보가 포함되지 않았는지 확인한다.
- 필요한 경우 비밀 값 자체가 아니라 키 이름과 설정 방법만 문서화한다.

## 검증 및 보고

- 변경 파일, 실행한 검사/빌드, 결과, 미실행 항목과 위험을 보고한다.
- 테스트가 없는 프로젝트는 가능한 정적 검사·빌드·수동 확인으로 대체하고 그 사실을 명시한다.
- 여러 저장소 작업이면 저장소별 결과와 의존 순서를 분리해 보고한다.

<!-- WORKSPACE-COMMON:END -->

## 프로젝트별 컨텍스트

아래 항목은 저장소를 처음 등록할 때 실제 내용을 확인해 채운다. 추측으로 작성하지 않는다.

- 기술 스택 및 런타임:
- 주요 소스 디렉터리:
- 빌드 명령:
- 테스트·정적 검사 명령:
- 로컬 실행 명령:
- 아키텍처·데이터 저장소·외부 연동:
- 알려진 함정과 금지 사항:
- 프로젝트 고유의 배포/운영 절차(있는 경우):
