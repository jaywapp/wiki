# 웹 리더 성능·오류 방어 설계

한국어 숫자 정렬 Collator를 재사용하고 정렬에 필요한 날짜는 문서당 한 번만 계산한다. 검색어가 없으면 본문 정규화를 건너뛴다. 정렬 tie-break는 기존 경로 순서를 유지한다. 저장된 설정의 JSON 파싱 결과가 객체인 경우만 사용하고 손상·null·배열은 빈 객체로 대체한다. 설정 원문을 로그에 남기지 않는다.

변경 전 검색 알고리즘의 결과를 fixture로 비교하고 설정 파싱은 독립 모듈에서 테스트한다. UI 구조·CSS·탐색 동작은 유지한다.
