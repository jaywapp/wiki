# 위키 웹 리더 분석

- orchestrator: Codex
- 요청: GitHub 문서를 웹으로 호스팅하고 검색, 트리뷰, 리스트뷰, 필터와 정렬 제공.
- 현재 상태: 기존 웹 앱이나 package.json 없이 Markdown 중심의 저장소. 현재 브랜치는 codex/workspace-environment-20260904이며 README 및 docs에 기존 미커밋 변경이 있다. 보존한다.
- 근거: README.md, SUMMARY.md, git ls-files, 공개 GitHub 저장소 조회. 추적 Markdown은 122개(운영 지침 포함). 원격 기본 브랜치 화면과 로컬의 ai/분류가 다르다.
- 원본 결정: 사용자 최초 요청의 공개 GitHub URL을 기준으로 한다. 별도 로컬 문서 포함 답변이 없으므로 미커밋 자료는 제외한다. GitHub API로 public/develop을 확인했다.
- 샘플 범위: 공개 문서의 제목과 내용을 참고한 동일한 소수 예시 자료, 검색·필터·정렬·보기 전환·본문 읽기 체험. 실제 저장소 동기화는 하지 않는다. 날짜는 시연 데이터로 명시한다.
- 제안: 문서 읽기 전용 정적 사이트. Git을 원본으로 유지하고 빌드 시 본문 검색 인덱스를 만든다. 사용자 계정이나 서버 저장이 필요하지 않아 Supabase 제외.
- 기본 필터: 폴더/분류. 정렬: 최신 수정순, 오래된순, 제목 오름차순/내림차순. 검색 범위: 제목·경로·본문. 변경 날짜는 파일 시스템 시각 대신 Git 이력을 사용한다.
- 비범위: 온라인 편집, 회원 시스템, 원본 문서 정리, 다른 저장소 변경.
- 완료 기준: 선택한 콘셉트로 전체 문서를 읽고 검색할 수 있음, 상대 링크·코드·표와 모바일 지원, 필터/정렬을 트리와 리스트 모두 적용, 배포 URL 제공.
- 게이트 해제: 사용자 B 선택으로 그래파이트 카탈로그 확정. 제시한 기능과 설계에 따라 운영 구현을 진행한다. 사용자 호스팅 요청에 따라 배포하되 commit/push는 하지 않는다.

## 콘텐츠 조사 결과

- 이전 결정: 자료 카탈로그 레이아웃의 추가 스타일 3종을 요청했다. 이후 B 선택으로 운영 구현 게이트를 해제했다. 모바일 이용 빈도는 계속 핵심 조건이다.

- 사용자 추가 요구: 모바일에서 자주 이용할 예정. 모바일 탐색과 본문 읽기를 핵심 사용 흐름으로 취급한다. localhost에 접근할 수 없는 상황이므로 데스크톱 및 모바일 캡처를 대화에 직접 제공한다.

- 122개 추적 Markdown 모두 첫 H1이 있다. 29개는 title/tags/updated 프론트매터를 가진다.
- 코드 블록 96개 문서, 표 83개 문서, Mermaid 15개 문서. Markdown/HTML 이미지는 표본 조사에서 0개.
- 로컬 실제 경로는 ai/인데 README의 ax/ai/ 링크와 SUMMARY의 ax/, ai-workflow/ 링크가 남아 있다. source-relative 기준 깨진 링크 72건이 보고되었다. 운영 단계에서 확정 원본 기준으로 재검증하고 알려진 이동만 별칭으로 처리한다. 원본 자체를 임의로 재구성하지 않는다.
- 참고 경로: ai/news/gpt-6-astra.md, git/monorepo-subtree.md, dotnet/wpf-implicit-usings-system-io.md, ue5/build-env/.

## 완료 결과

2026-09-09 B 그래파이트 카탈로그를 https://jaywapp-wiki.vercel.app 에 배포했다. 공개 develop 커밋 `15a426bf456eecb185c4ac39dc774ee97af5a69e`에서 145개 문서를 수집했다. 초기 로컬 조사 수와 다른 이유는 공개 원격 커밋을 운영 원본으로 확정했기 때문이다.

검색·폴더/태그 필터·정렬·트리/리스트·모바일 탐색/읽기·직접 문서 주소를 구현했다. 목록 행에는 제목·태그·짧은 설명을 표시한다. 20개 테스트와 빌드, 공개 HTTP 200 및 모바일 브라우저 검증을 통과했다. 자동 Git 배포 연결은 이어서 완료한다.
