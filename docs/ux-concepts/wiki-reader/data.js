window.sampleDocuments = [
  {id:'subtree', category:'Git', path:'git/monorepo-subtree.md', title:'Git Subtree로 모노레포 구성하기', date:'2026-09-06', description:'여러 저장소를 하나의 모노레포로 통합하는 패턴과 자주 쓰는 명령어.', body:'<p>여러 레포를 하나의 모노레포로 통합할 때 사용하는 패턴입니다. 기존 저장소의 이력을 유지하면서 디렉터리 단위로 가져올 수 있습니다.</p><h2>레포 편입</h2><p>원본 저장소를 하위 디렉터리에 추가합니다. <code>--squash</code>는 원본 커밋 이력을 하나로 압축합니다.</p><pre><code>git subtree add --prefix=packages/library \\\n  https://github.com/org/library.git main --squash</code></pre><h2>업데이트 가져오기</h2><p>원본 저장소에 새 변경이 생기면 같은 prefix를 사용해 가져옵니다.</p><pre><code>git subtree pull --prefix=packages/library \\\n  https://github.com/org/library.git main --squash</code></pre><h2>기억할 점</h2><p>prefix와 원본 브랜치를 일관되게 유지하세요. 로컬 변경이 있다면 병합 충돌을 먼저 해결합니다.</p>'},
  {id:'wpf', category:'.NET', path:'dotnet/wpf-implicit-usings-system-io.md', title:'WPF에서 Path와 File을 찾지 못할 때', date:'2026-09-04', description:'UseWPF 프로젝트의 암시적 using 차이와 CS0103 오류 해결.', body:'<p>일반 .NET 프로젝트에서는 동작하던 Path.Combine과 File.Exists가 WPF 프로젝트에서는 컴파일 오류를 낼 수 있습니다.</p><h2>원인</h2><p>WPF SDK는 암시적 전역 using 목록에서 System.IO와 System.Net.Http를 제거합니다. 네임스페이스 간 타입 충돌을 피하기 위한 동작입니다.</p><h2>해결</h2><pre><code>using System.IO;\n\nvar fullPath = Path.Combine(basePath, fileName);</code></pre><p>필요한 네임스페이스를 명시적으로 추가합니다.</p>'},
  {id:'skills', category:'AI / AX', path:'ax/design-skills-comparison.md', title:'AI UI/UX 디자인 스킬 비교', date:'2026-09-03', description:'Taste, UI UX Pro Max, Impeccable의 역할과 활용 시점 정리.', body:'<p>디자인 스킬은 방향 설정, 구현 가이드, 품질 검증 등 서로 다른 역할을 수행합니다.</p><h2>작업에 맞는 역할 선택</h2><p>새 화면을 만들 때는 먼저 사용자와 핵심 동작을 정리합니다. 구현 후에는 가독성, 접근성, 반응형 동작을 확인합니다.</p><h2>비교 관점</h2><table><thead><tr><th>관점</th><th>확인할 내용</th></tr></thead><tbody><tr><td>방향</td><td>제품의 성격과 시각적 일관성</td></tr><tr><td>구현</td><td>컴포넌트와 인터랙션</td></tr><tr><td>검증</td><td>접근성과 실제 사용 흐름</td></tr></tbody></table>'},
  {id:'feedback', category:'Architecture', path:'architecture/tokenless-feedback-vercel-proxy.md', title:'토큰 없는 데스크톱 앱 피드백 구조', date:'2026-09-02', description:'앱에 GitHub 토큰을 넣지 않고 서버 프록시로 이슈를 등록하는 구조.', body:'<p>배포되는 클라이언트에 비밀 토큰을 포함하지 않고 피드백을 처리하는 구조입니다.</p><h2>요청 흐름</h2><p>데스크톱 앱이 서버에 피드백을 전달하고 서버가 입력을 검증한 뒤 GitHub API를 호출합니다.</p><h2>서버의 책임</h2><p>입력 검증, 요청 제한, 비밀 관리와 오류 응답을 서버 경계에서 처리합니다.</p>'},
  {id:'ue5', category:'Unreal Engine', path:'ue5/ue5-build-farm.md', title:'UE5 빌드 팜 구성 키워드', date:'2026-08-29', description:'Horde, DDC, Zen Streaming과 빌드 환경을 이해하기 위한 출발점.', body:'<p>Unreal Engine 빌드 환경을 구성할 때 만나는 주요 요소를 정리합니다.</p><h2>공유되는 작업</h2><p>빌드, 파생 데이터 캐시, 셰이더 컴파일을 구분해 병목을 살펴봅니다.</p><h2>관측부터 시작하기</h2><p>실행 시간과 캐시 적중 여부를 기록해 어떤 구간을 먼저 개선할지 판단합니다.</p>'},
  {id:'teamcity', category:'CI / CD', path:'teamcity/teamcity-perforce-workspace-optimization.md', title:'TeamCity와 Perforce 작업 공간 최적화', date:'2026-08-27', description:'반복 동기화 비용을 줄이는 Persistent Workspace 설계 메모.', body:'<p>CI 작업에서 반복되는 Perforce 동기화 비용을 줄이기 위한 작업 공간 설계입니다.</p><h2>작업 공간 유지</h2><p>매번 전체 파일을 내려받는 대신 상태를 관리하면서 필요한 변경을 동기화하는 방향을 검토합니다.</p><h2>역할 경계</h2><p>빌드 실행과 동기화, 작업 공간 정리의 책임을 구분합니다.</p>'}
];
